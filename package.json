{
  "name": "codex-clone",
  "displayName": "Codex Clone",
  "description": "A VS Code coding chat powered by xKiro's OpenAI GPT-5.3-Codex-Spark model.",
  "version": "0.0.7",
  "publisher": "inukaminsara",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": ["Other"],
  "activationEvents": [
    "onCommand:codexClone.openChat",
    "onCommand:codexClone.setApiKey",
    "onCommand:codexClone.clearApiKey",
    "onCommand:codexClone.checkConnection"
  ],
  "main": "./out/extensionV2.js",
  "contributes": {
    "commands": [
      { "command": "codexClone.openChat", "title": "Codex Clone: Open Chat" },
      { "command": "codexClone.setApiKey", "title": "Codex Clone: Set xKiro API Key" },
      { "command": "codexClone.clearApiKey", "title": "Codex Clone: Clear xKiro API Key" },
      { "command": "codexClone.checkConnection", "title": "Codex Clone: Test xKiro Connection" }
    ],
    "menus": {
      "commandPalette": [
        { "command": "codexClone.openChat" },
        { "command": "codexClone.setApiKey" },
        { "command": "codexClone.clearApiKey" },
        { "command": "codexClone.checkConnection" }
      ]
    },
    "configuration": {
      "title": "Codex Clone",
      "properties": {
        "codexClone.model": {
          "type": "string",
          "default": "openai/gpt-5.3-codex-spark",
          "description": "xKiro model ID used by Codex Clone."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile",
    "package": "npm run compile && npx @vscode/vsce package",
    "web": "node web-server.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.5.0",
    "@vscode/vsce": "^3.2.1"
  }
}
